// App version — single source of truth. Bump the last digits on each zip/deploy
// so you can confirm on the live site which build is running.
// Format: MAJOR.MINOR.PATCH  (e.g. 1.0.2 → next 1.0.3)
export const APP_VERSION = "1.0.60";
